{
  "name": "@ethereum/lean-multisig",
  "version": "0.0.0-dev",
  "description": "Node.js bindings for leanMultisig",
  "license": "MIT",
  "main": "facade.js",
  "types": "facade.d.ts",
  "napi": {
    "binaryName": "lean_multisig_node"
  },
  "scripts": {
    "build": "napi build --platform --release",
    "test": "node --test"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.8.6"
  }
}
